# Copy to .env and set values as needed. Safe defaults ensure local run without keys.

# Firebase (optional at runtime in dev)
FIREBASE_API_KEY=
FIREBASE_APP_ID=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_IOS_BUNDLE_ID=
FIREBASE_ANDROID_APP_ID=

# OpenAI (required for OCR parsing)
OPENAI_API_KEY=

# Subscription product IDs (set per store)
IOS_SUBSCRIPTION_MONTHLY_ID=
IOS_SUBSCRIPTION_YEARLY_ID=
ANDROID_SUBSCRIPTION_MONTHLY_ID=
ANDROID_SUBSCRIPTION_YEARLY_ID=
